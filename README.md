# 🚀 Super-Agent Telegram Bot (Gemini Function Calling)

این پروژه یک ربات تلگرام پیشرفته است که با استفاده از قابلیت **Function Calling** مدل **Google Gemini 2.5 Flash**، به یک **Super-Agent** چندمنظوره تبدیل شده است. این ربات می‌تواند به صورت هوشمند، درخواست‌های کاربر را به ماژول‌های تخصصی هدایت کرده و پاسخ‌های دقیق و به‌روز ارائه دهد.

## ✨ قابلیت‌های کلیدی (Tools)

ربات به صورت هوشمند از ابزارهای زیر برای پاسخگویی استفاده می‌کند:

| ماژول | فایل | قابلیت |
| :--- | :--- | :--- |
| **📈 Trader** | `services/trader.py` | دریافت قیمت‌های لحظه‌ای ارزهای دیجیتال (BTC, ETH) از API واقعی بایننس. |
| **⚖️ Legal** | `services/legal.py` | ارائه اطلاعات حقوقی ساختاریافته در مورد قوانین ایران (ارث، مهریه، چک). |
| **✍️ Writer** | `services/writer.py` | تولید محتوای متنی در سبک‌های مختلف (رسمی، کپشن شبکه‌های اجتماعی، مقاله). |
| **📚 Tutor** | `services/tutor.py` | ارائه توضیحات آموزشی ساده و کاربردی برای مفاهیم مختلف. |
| **🔒 Ethics** | `services/ethics.py` | فیلتر کردن درخواست‌های غیرقانونی یا غیراخلاقی قبل از پردازش توسط هوش مصنوعی. |
| **🖼️ Image Gen** | `services/image_generator.py` | شبیه‌سازی تولید تصویر و ذخیره آن در سیستم فایل (نیاز به API تولید تصویر واقعی). |
| **💎 Premium** | `services/premium.py` | مدیریت سطح دسترسی کاربران (آماده‌سازی برای سیستم اشتراک پولی). |

## 🛠️ تکنولوژی‌های استفاده شده

*   **هوش مصنوعی:** Google Gemini 2.5 Flash (با قابلیت Function Calling)
*   **زبان برنامه‌نویسی:** Python
*   **فریم‌ورک وب:** Flask
*   **کتابخانه تلگرام:** PyTelegramBotAPI
*   **استقرار:** Gunicorn (برای اجرای وب‌هوک)

## ⚙️ راهنمای استقرار (Deployment)

این پروژه برای استقرار آسان از طریق Webhook در پلتفرم‌هایی مانند **Render** یا **Railway** بهینه‌سازی شده است.

### ۱. متغیرهای محیطی (Environment Variables)

شما باید متغیرهای زیر را در پنل کاربری پلتفرم استقرار خود تنظیم کنید:

| متغیر | توضیحات |
| :--- | :--- |
| `TELEGRAM_TOKEN` | توکن ربات تلگرام شما (دریافت شده از BotFather). |
| `GEMINI_API_KEY` | کلید API گوگل جمینای شما. |
| `WEBHOOK_URL` | آدرس URL کامل سرویس شما (مثلاً `https://my-ai-bot.onrender.com`). |

### ۲. دستور اجرای (Procfile)

فایل `Procfile` برای اجرای سرور Gunicorn تنظیم شده است:

```
web: gunicorn main:app
```

### ۳. نصب وابستگی‌ها

تمام وابستگی‌های مورد نیاز در فایل `requirements.txt` لیست شده‌اند:

```
pyTelegramBotAPI
flask
google-generativeai
python-dotenv
requests
gunicorn
```

---
**توسعه‌دهنده:** [Ariyan3323](https://github.com/Ariyan3323)
**تاریخ آخرین به‌روزرسانی:** دسامبر ۲۰۲۵
